<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabalho_01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trabalho_01">
        <activity android:name=".grid"></activity>
        <activity android:name=".popMenu" />
        <activity android:name=".PlayMusic" />
        <activity
            android:name=".Tabbed"
            android:label="@string/title_activity_tabbed"
            android:theme="@style/Theme.Trabalho_01.NoActionBar" />
        <activity android:name=".TextSpinner" />
        <activity android:name=".CliqueLongo" />
        <activity android:name=".toggleButton" />
        <activity android:name=".Features" />
        <activity android:name=".ListaTarefas" />
        <activity android:name=".VisualizarTarefas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>